#!/bin/sh
# Pre-push hook: typecheck + lint до отправки в remote.
# Установка: npm run prepare  (или git config core.hooksPath .githooks)

set -e

echo "→ pre-push: typecheck..."
npm run typecheck

echo "→ pre-push: lint..."
npm run lint

echo "✓ pre-push: OK"
