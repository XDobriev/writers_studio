# Roadmap

## Приоритет 1 — горящие баги

Смотри [bugs.md](bugs.md) — баги #4 (sidebar-навигация) и #6 (логотип) мешают базовому UX.

## Приоритет 2 — Заход 14 (writing snapshots)

- Применить миграцию `0008_writing_snapshots.sql` в Supabase.
- Подключить чтение в дэшборде: heatmap активности, серия дней, накопленный объём.
- Убрать фейковые данные из статусной плашки редактора (баг #5).

## Приоритет 3 — визуализация

- Графическая карта мира по координатам `x/y` из таблицы `locations`.
- Горизонтальная timeline по полю `pos` из `timeline_events`.

## Приоритет 4 — экспорт расширенный

- DOCX через библиотеку (docx.js или аналог).
- EPUB через epub.js или аналог.

## Приоритет 5 — обложка книги

См. [bugs.md](bugs.md) раздел «Запланированные фичи».

## Приоритет 6 — заметки на полях

Баг #2: реальный CRUD для заметок. Таблица `notes` в Supabase.

## Backlog

- Telegram OAuth (кнопка скрыта — см. project-state.md).
- Drag-n-drop порядка глав.
- Scenes и иерархия внутри глав.
- Горизонтальный timeline Gantt.
